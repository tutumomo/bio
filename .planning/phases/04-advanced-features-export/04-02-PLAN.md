---
phase: 04-advanced-features-export
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [backend/services/ensembl.py, backend/services/gene_pipeline.py, backend/api/genes.py, backend/schemas/gene.py, backend/tests/test_ensembl.py]
autonomous: true
requirements: [API-03, UI-05]

must_haves:
  truths:
    - "API endpoint /api/genes/{gene_symbol}/expression returns tissue expression data."
    - "Backend successfully fetches median expression values from GTEx/Ensembl."
  artifacts:
    - path: "backend/services/ensembl.py"
      provides: "get_tissue_expression method"
    - path: "backend/schemas/gene.py"
      provides: "TissueExpression schemas"
  key_links:
    - from: "backend/api/genes.py"
      to: "backend/services/gene_pipeline.py"
      via: "get_tissue_expression call"
---

<objective>
Implement the backend service and API endpoint for fetching gene tissue expression data.

Purpose: Provide the raw data required for the detail view's expression chart.
Output: A functional REST endpoint returning tissue-specific expression (TPM) values.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/services/ensembl.py
@backend/services/gene_pipeline.py
@backend/api/genes.py
@backend/schemas/gene.py

<interfaces>
From backend/services/ensembl.py:
```python
class EnsemblClient:
    async def get_ensembl_id(self, gene_symbol: str) -> Optional[str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ensembl Tissue Expression Service</name>
  <files>backend/services/ensembl.py, backend/tests/test_ensembl.py</files>
  <behavior>
    - Test: get_tissue_expression("ENSG00000139618") returns a list of tissue/expression pairs.
    - Handle cases where no expression data is found (return empty list).
    - Use the GTEx public API: https://gtexportal.org/api/v2/expression/medianGeneExpression?gencodeId={ensembl_id}&tissueSiteDetailId=all&format=json
  </behavior>
  <action>
    - Add `get_tissue_expression(ensembl_id: str)` to `EnsemblClient`.
    - implementation: Fetch from GTEx API, parse the JSON response.
    - Map GTEx `tissueSiteDetailId` (or similar) to a readable name if necessary.
    - Return a list of `{"tissue": str, "tpm": float}`.
  </action>
  <verify>
    <automated>pytest backend/tests/test_ensembl.py</automated>
  </verify>
  <done>EnsemblClient can retrieve tissue-level expression data.</done>
</task>

<task type="auto">
  <name>Task 2: Gene Expression API Endpoint</name>
  <files>backend/api/genes.py, backend/services/gene_pipeline.py, backend/schemas/gene.py</files>
  <action>
    - Define `TissueExpressionEntry(BaseModel)` and `TissueExpressionResult(BaseModel)` in `backend/schemas/gene.py`.
    - Add `get_tissue_expression(gene_symbol, ensembl_id)` to `GenePipeline`.
    - Add `GET /api/genes/{gene_symbol}/expression` endpoint to `backend/api/genes.py`.
    - The endpoint should first ensure we have an `ensembl_id` (look up in cache or NCBI if not provided).
    - Fetch expression data using `ensembl.get_tissue_expression`.
  </action>
  <verify>
    <automated>curl -s http://localhost:8000/api/genes/BRCA1/expression | grep "tpm"</automated>
  </verify>
  <done>Expression data is available via REST API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → External | Outgoing request to GTEx API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | GTEx request | accept | Public data source, no secrets leaked. |
| T-04-02-02 | DoS | EnsemblClient | mitigate | Semaphore usage (already implemented in client) |
</threat_model>

<verification>
1. Verify `pytest backend/tests/test_ensembl.py` passes.
2. Manually test the new endpoint with a known Ensembl ID (e.g., ENSG00000139618).
</verification>

<success_criteria>
- Backend returns a list of tissues and TPM values for a given gene.
- Data format is compatible with frontend chart requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-features-export/04-02-SUMMARY.md`
</output>
