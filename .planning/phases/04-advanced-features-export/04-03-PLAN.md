---
phase: 04-advanced-features-export
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [frontend/src/components/TissueExpressionChart.tsx, frontend/src/pages/GeneDetailPage.tsx, frontend/src/lib/api.ts]
autonomous: false
requirements: [UI-05]

must_haves:
  truths:
    - "Gene Detail page displays a responsive tissue expression chart."
    - "Chart shows actual TPM values fetched from the backend API."
  artifacts:
    - path: "frontend/src/components/TissueExpressionChart.tsx"
      provides: "Bar chart component"
    - path: "frontend/src/pages/GeneDetailPage.tsx"
      provides: "Detail view with expression chart section"
  key_links:
    - from: "frontend/src/pages/GeneDetailPage.tsx"
      to: "backend/api/genes.py"
      via: "fetch(api/genes/{symbol}/expression)"
---

<objective>
Visualize tissue expression data in the gene detail view using a responsive bar chart.

Purpose: Provide biological context for gene activity across various human tissues.
Output: A professional, interactive bar chart on the Gene Detail page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/pages/GeneDetailPage.tsx
@frontend/src/lib/api.ts
@frontend/package.json

<interfaces>
From frontend/src/lib/api.ts:
```typescript
export interface TissueExpressionEntry {
  tissue: string;
  tpm: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tissue Expression Chart Component</name>
  <files>frontend/src/components/TissueExpressionChart.tsx, frontend/src/lib/api.ts</files>
  <action>
    - Add `getTissueExpression(symbol: string)` to `api` in `frontend/src/lib/api.ts`.
    - Create `frontend/src/components/TissueExpressionChart.tsx`.
    - implementation: Use `recharts` (`BarChart`, `XAxis`, `YAxis`, `Tooltip`, `ResponsiveContainer`).
    - Sort tissues by TPM (descending) before rendering.
    - If too many tissues, consider a scrollable container or limiting to top 30.
    - Professional styling: Use a consistent primary color (e.g., `#002045` or Emerald) and clean axis labels.
  </action>
  <verify>
    <automated>grep "BarChart" frontend/src/components/TissueExpressionChart.tsx</automated>
  </verify>
  <done>Reusable TissueExpressionChart component is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Chart into Gene Detail Page</name>
  <files>frontend/src/pages/GeneDetailPage.tsx</files>
  <action>
    - Import and render `TissueExpressionChart` in `GeneDetailPage.tsx`.
    - Placement: Between the Gene Info Card and the Variant Table.
    - Wrap in a white card with appropriate padding and a title "Tissue Expression (GTEx)".
    - Pass the gene symbol and handle loading/empty states gracefully.
  </action>
  <verify>
    <automated>grep "TissueExpressionChart" frontend/src/pages/GeneDetailPage.tsx</automated>
  </verify>
  <done>Gene Detail page displays the expression visualization.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Advanced features: Autocomplete, CSV Export, and Tissue Expression Chart.</what-built>
  <how-to-verify>
    1. Search for "BRCA1" and verify autocomplete suggestions appear.
    2. Navigate to BRCA1 Detail page.
    3. Verify the Tissue Expression chart renders correctly with real data.
    4. Click "Download CSV" and verify the file content matches "Supplementary Table 1" format.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Client | JSON payload containing TPM values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | Tissue expression data | accept | Public biological data, non-sensitive. |
| T-04-03-02 | Client-side CPU Exhaustion | TissueExpressionChart | mitigate | Limit number of bars or use virtualization if dataset exceeds 100 entries. |
</threat_model>

<verification>
1. Check that `TissueExpressionChart.tsx` exists and uses Recharts.
2. Check that `GeneDetailPage.tsx` imports and renders it.
3. Verify responsiveness on mobile/desktop.
</verification>

<success_criteria>
- Gene Detail page includes a functional bar chart showing TPM levels across tissues.
- UI remains performant even with large expression datasets.
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-features-export/04-03-SUMMARY.md`
</output>
