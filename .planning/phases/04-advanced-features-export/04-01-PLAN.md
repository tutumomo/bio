---
phase: 04-advanced-features-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/services/gene_pipeline.py, backend/api/genes.py, frontend/src/components/VariantTable.tsx, backend/tests/test_genes.py]
autonomous: true
requirements: [API-03, UI-07]

must_haves:
  truths:
    - "Search bar shows autocomplete suggestions from the GeneCache table."
    - "Clicking 'Download CSV' in VariantTable downloads a file with correct headers."
  artifacts:
    - path: "backend/services/gene_pipeline.py"
      provides: "autocomplete_genes method"
    - path: "backend/api/genes.py"
      provides: "GET /api/genes/autocomplete endpoint"
    - path: "frontend/src/components/VariantTable.tsx"
      provides: "CSV Export button"
  key_links:
    - from: "frontend/src/components/VariantTable.tsx"
      to: "frontend/src/lib/export.ts"
      via: "exportCSV call"
---

<objective>
Refactor the autocomplete endpoint to query the local cache using optimized ILIKE queries and add a CSV export button to the variant results table.

Purpose: Improve user search experience and enable data portability for researchers.
Output: Faster autocomplete suggestions and functional CSV export.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/services/gene_pipeline.py
@backend/api/genes.py
@frontend/src/components/VariantTable.tsx
@frontend/src/lib/export.ts

<interfaces>
From backend/services/gene_pipeline.py:
```python
async def search_genes_cached(self, query: str, db: AsyncSession) -> List[Dict]: ...
```

From frontend/src/lib/export.ts:
```typescript
export function exportCSV(headers: string[], rows: string[][], filename: string): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend Autocomplete Refactor</name>
  <files>backend/services/gene_pipeline.py, backend/api/genes.py, backend/tests/test_genes.py</files>
  <behavior>
    - Test: GET /api/genes/autocomplete?q=BRCA returns BRCA1, BRCA2 if in cache.
    - Test: ILIKE search works on both symbol (starts-with) and full_name (contains).
    - Limit results to top 10.
  </behavior>
  <action>
    - Add `autocomplete_genes(q: str, db: AsyncSession)` method to `GenePipeline`.
    - Implementation: `select(GeneCache).where((GeneCache.symbol.ilike(f"{q}%")) | (GeneCache.full_name.ilike(f"%{q}%"))).limit(10)`.
    - Update `backend/api/genes.py`: `autocomplete` endpoint should call this new method instead of `search_genes_cached`.
    - Add/update tests in `backend/tests/test_genes.py` to verify autocomplete logic.
  </action>
  <verify>
    <automated>pytest backend/tests/test_genes.py -k autocomplete</automated>
  </verify>
  <done>Autocomplete returns partial matches from cache in under 50ms.</done>
</task>

<task type="auto">
  <name>Task 2: Variant Table CSV Export</name>
  <files>frontend/src/components/VariantTable.tsx</files>
  <action>
    - Add a "Download CSV" button next to the "Showing X variants" footer or in a new header section.
    - Style the button to match the project's aesthetic (e.g., secondary border button with a Download icon).
    - Implement the `onExport` handler:
      - Prepare headers: `["RSID", "Gene", "Consequence", "Impact", "CADD", "GERP++", "RegulomeDB"]`.
      - Map `variants` prop to string rows.
      - Call `exportCSV` from `@/lib/export`.
    - Filename should be `variant_annotations_{gene_symbol}.csv`.
  </action>
  <verify>
    <automated>grep "exportCSV" frontend/src/components/VariantTable.tsx</automated>
  </verify>
  <done>Button exists and triggers CSV download with filtered data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Autocomplete query parameter is untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | GET /api/genes/autocomplete | mitigate | Use SQLAlchemy parameter binding for ILIKE (prevent SQLi) |
| T-04-01-02 | DoS | Autocomplete endpoint | mitigate | Rate limiting via slowapi (already implemented globally) |
</threat_model>

<verification>
1. Run `pytest backend/tests/test_genes.py` and ensure all tests pass.
2. Manually verify search bar suggestions appear when typing.
3. Manually verify CSV download contains the expected variant columns.
</verification>

<success_criteria>
- Autocomplete suggests "BRCA1" when typing "BRC".
- CSV export includes all variant metadata for the current gene.
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-features-export/04-01-SUMMARY.md`
</output>
