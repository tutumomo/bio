---
phase: 03-authentication-history
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [frontend/src/pages/HistoryPage.tsx, frontend/src/pages/ResultsPage.tsx, frontend/src/lib/api.ts]
autonomous: true
requirements: [UI-06, AUTH-03]

must_haves:
  truths:
    - "History entries correctly link to the results view"
    - "Clicking an entry re-applies previous filters and query"
    - "The History page supports infinite scroll or paginated buttons"
  artifacts:
    - path: "frontend/src/pages/HistoryPage.tsx"
      provides: "User-facing history view"
    - path: "frontend/src/pages/ResultsPage.tsx"
      provides: "Filter-replaying results view"
  key_links:
    - from: "frontend/src/pages/HistoryPage.tsx"
      to: "frontend/src/pages/ResultsPage.tsx"
      via: "react-router-dom navigation with URL params"
---

<objective>
Enhance the History page UI to support pagination and implement "Click-to-replay" functionality to restore previous search parameters and filters.

Purpose: Provide a seamless user experience for revisiting research results.
Output: Dynamic history browsing and filter-aware results replay.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-authentication-history/03-CONTEXT.md
@frontend/src/pages/HistoryPage.tsx
@frontend/src/pages/ResultsPage.tsx
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Update API client for History pagination</name>
  <files>frontend/src/lib/api.ts</files>
  <action>
    - Update `getHistory` to accept `limit` and `offset`.
    - Change path to `/api/users/me/history` (per requirement).
    - Ensure `getMe` also points to `/api/users/me` if prefix changed.
    - Add/update any other history methods if necessary.
  </action>
  <verify>
    <automated>Check network tab for correct API calls.</automated>
  </verify>
  <done>Frontend API client matches the new backend paths and supports pagination params.</done>
</task>

<task type="auto">
  <name>Sync Filters to URL in ResultsPage</name>
  <files>frontend/src/pages/ResultsPage.tsx</files>
  <action>
    - Refactor `filters` state management to derive initial state from URL search parameters.
    - Update the URL (using `setSearchParams`) whenever filters are changed.
    - Ensure all relevant filters (CADD, GERP, Impact, etc.) are serializable and reflected in the URL.
    - Note: This enables sharing the search URL and replaying it via HistoryPage.
  </action>
  <verify>
    <automated>Change a filter and verify URL query parameters change.</automated>
  </verify>
  <done>ResultsPage filters are synchronized with the URL.</done>
</task>

<task type="auto">
  <name>Implement History Page Pagination and Replay</name>
  <files>frontend/src/pages/HistoryPage.tsx</files>
  <action>
    - Update the HistoryPage to use TanStack Query with pagination (limit/offset).
    - Provide "Load More" button or paginated navigation.
    - Update the click handler for history entries to pass ALL stored filters as URL query parameters when navigating back to the `/results` page.
    - Display the date and time of the search more clearly.
  </action>
  <verify>
    <automated>Click a history entry and verify both query and filters are applied to results.</automated>
  </verify>
  <done>History page supports pagination and restores full search state (query + filters).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → User | Visibility of history data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | frontend/src/pages/HistoryPage.tsx | mitigate | Only show history data returned for the authenticated user |
</threat_model>

<verification>
Manual test of the full loop: perform search -> change filters -> view history -> click entry -> check results match.
</verification>

<success_criteria>
History page allows navigating through large search logs and perfectly restores previous research states.
</success_criteria>

<output>
After completion, create `.planning/phases/03-authentication-history/03-03-SUMMARY.md`
</output>
