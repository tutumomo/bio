---
phase: 03-authentication-history
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [backend/api/genes.py, backend/api/users.py, backend/main.py]
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "Search history includes gene counts and filters if provided"
    - "History is paginated (limit and offset)"
    - "History is accessible at /api/users/me/history"
  artifacts:
    - path: "backend/api/users.py"
      provides: "Paginated history endpoint"
  key_links:
    - from: "backend/api/genes.py"
      to: "backend/models/user.py"
      via: "SearchHistory record creation"
---

<objective>
Implement comprehensive search history logging and provide a paginated API endpoint to retrieve history entries.

Purpose: Allow users to review and revisit their previous research sessions.
Output: Reliable history logging and a paginated history API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-authentication-history/03-CONTEXT.md
@backend/api/genes.py
@backend/api/users.py
@backend/models/user.py
@backend/api/users.py
</context>

<tasks>

<task type="auto">
  <name>Refactor History Logging in Search Genes</name>
  <files>backend/api/genes.py</files>
  <action>
    - Ensure `search_genes` uses the refactored `User` model dependency (from Plan 01).
    - Capture search metadata (gene_count) and any optional filters passed to the search (if any).
    - Ensure `user.id` is used directly from the model object.
    - Check if a duplicate query within a short timeframe should be ignored or update the timestamp.
  </action>
  <verify>
    <automated>Perform search via API and check SearchHistory table.</automated>
  </verify>
  <done>History is logged with correct User.id and gene counts.</done>
</task>

<task type="auto">
  <name>Implement paginated /api/users/me/history</name>
  <files>backend/api/users.py, backend/main.py</files>
  <action>
    - Update `backend/api/users.py` router to use `/api/users` prefix (per requirement).
    - Add/Refactor `@router.get("/me/history")` endpoint.
    - Add `limit: int = 50` and `offset: int = 0` query parameters.
    - Fetch history for the current user, ordered by `searched_at` descending.
    - Return history entries and a total count for pagination.
    - Update `backend/main.py` to include the router with new prefix if necessary.
  </action>
  <verify>
    <automated>curl http://localhost:8000/api/users/me/history?limit=10&offset=0</automated>
  </verify>
  <done>History API is paginated and uses the requested URL path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → History API | Personal search data retrieval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | backend/api/users.py | mitigate | Ensure history is strictly filtered by current user's ID |
| T-03-02-02 | Denial of Service | backend/api/users.py | mitigate | Enforce pagination limits (max 100 per page) |
</threat_model>

<verification>
Manual test with multiple searches and then call the history API with different limit/offset parameters.
</verification>

<success_criteria>
User can retrieve their history in chunks (pagination) and each entry contains the search metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/03-authentication-history/03-02-SUMMARY.md`
</output>
