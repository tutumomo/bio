---
phase: 05-production-deployment-polish
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: [frontend/src/index.css, frontend/src/components/ThemeToggle.tsx, frontend/src/App.tsx, frontend/src/components/Layout.tsx, frontend/src/components/GeneTable.tsx, frontend/src/components/VariantTable.tsx, frontend/src/components/FilterPanel.tsx, frontend/src/components/SearchBar.tsx]
autonomous: true
requirements: [DEPLOY-02]
must_haves:
  truths:
    - "User can toggle between dark and light modes"
    - "Dark mode persists after page refresh"
    - "System dark mode preference is respected by default"
    - "All UI components have readable contrast in dark mode"
  artifacts:
    - path: "frontend/src/components/ThemeToggle.tsx"
      provides: "UI switch for themes"
    - path: "frontend/src/index.css"
      provides: "Tailwind v4 theme variables"
  key_links:
    - from: "frontend/src/index.css"
      to: "dark variant"
      via: "@theme or .dark class"
---

<objective>
Implement dark mode functionality and apply dark-theme variants to all UI components to ensure a high-quality visual experience.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-production-deployment-polish/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark Mode Setup & ThemeToggle</name>
  <files>frontend/src/index.css, frontend/src/components/ThemeToggle.tsx, frontend/src/App.tsx</files>
  <action>
    - Configure Tailwind v4 for dark mode in `frontend/src/index.css` by defining theme variables for light and dark contexts.
    - Implement a `ThemeToggle.tsx` component using `lucide-react` icons (Sun/Moon).
    - Use a `useTheme` hook or simple logic in `App.tsx` to manage the `.dark` class on the `<html>` or `<body>` element.
    - Ensure persistence using `localStorage` and respect the `prefers-color-scheme: dark` media query on initial load.
  </action>
  <verify>
    - Manually toggle the theme and confirm the `.dark` class is correctly applied/removed from the root element.
    - Confirm local storage is updated and used on refresh.
  </verify>
  <done>Dark mode infrastructure and toggle component are ready and integrated.</done>
</task>

<task type="auto">
  <name>Task 2: Dark Variant Application</name>
  <files>frontend/src/components/Layout.tsx, frontend/src/components/GeneTable.tsx, frontend/src/components/VariantTable.tsx, frontend/src/components/FilterPanel.tsx, frontend/src/components/SearchBar.tsx, frontend/src/components/ImpactBadge.tsx</files>
  <action>
    - Review all core components and add `dark:` variants to Tailwind classes.
    - Update `Layout.tsx` (background, text colors).
    - Update `GeneTable.tsx` and `VariantTable.tsx` (header colors, row borders, zebra striping).
    - Update `FilterPanel.tsx` (input fields, labels).
    - Update `SearchBar.tsx` (input background, border).
    - Ensure `ImpactBadge.tsx` and `CaddScoreBar.tsx` have appropriate visibility in dark mode.
  </action>
  <verify>
    - Switch to dark mode and visually inspect each component for readability and contrast.
  </verify>
  <done>All major UI components are fully themed for dark mode.</done>
</task>

<task type="auto">
  <name>Task 3: Final UI Polish & Responsive Review</name>
  <files>frontend/src/index.css, frontend/src/pages/SearchPage.tsx, frontend/src/pages/ResultsPage.tsx</files>
  <action>
    - Fix any leftover light-mode hardcodings (e.g., `text-black` instead of `text-slate-900 dark:text-slate-100`).
    - Verify that Recharts (`TissueExpressionChart`) colors are adjusted for dark mode if necessary (or use a theme-agnostic palette).
    - Ensure hover/active states in tables and buttons are clear in both modes.
    - Test responsive layouts in both themes to ensure no regressions.
  </action>
  <verify>
    - Perform a full walkthrough of the search-to-results-to-detail flow in both light and dark modes on multiple screen sizes.
  </verify>
  <done>UI/UX is polished, accessible, and fully responsive across both themes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A - Theme management is purely local/presentation logic.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | S | ThemeToggle | accept | Low impact; theme spoofing is non-harmful |
</threat_model>

<verification>
1. Verify dark mode toggle functionality and persistence.
2. Review contrast across all tables and forms in dark mode.
</verification>

<success_criteria>
- Dark mode is functional, persists in local storage, and respects system preferences.
- All components are readable and visually consistent in both themes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-production-deployment-polish/05-03-SUMMARY.md`
</output>
