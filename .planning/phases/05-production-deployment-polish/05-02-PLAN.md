---
phase: 05-production-deployment-polish
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [frontend/src/components/ErrorBoundary.tsx, frontend/src/components/SkeletonTable.tsx, frontend/src/components/SkeletonChart.tsx, frontend/src/pages/ResultsPage.tsx, frontend/src/pages/GeneDetailPage.tsx, frontend/src/App.tsx]
autonomous: true
requirements: [DEPLOY-02]
must_haves:
  truths:
    - "Application does not white-screen on rendering errors (ErrorBoundary shows helpful message)"
    - "Major tables and charts display animated skeleton loaders during data fetching"
  artifacts:
    - path: "frontend/src/components/ErrorBoundary.tsx"
      provides: "Global error protection"
    - path: "frontend/src/components/SkeletonTable.tsx"
      provides: "Loading state for tables"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "ErrorBoundary"
      via: "wrapping"
---

<objective>
Improve UX with skeleton loaders and implement error boundaries to handle component failures gracefully.
This plan focuses on perceived performance (skeletons) and stability (error boundaries).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-production-deployment-polish/05-CONTEXT.md

<interfaces>
From frontend/src/components/GeneTable.tsx:
```typescript
interface GeneTableProps {
  genes: Gene[];
  isLoading: boolean;
}
```

From frontend/src/components/SkeletonTable.tsx (existing draft):
```typescript
export default function SkeletonTable() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global Error Boundary</name>
  <files>frontend/src/components/ErrorBoundary.tsx, frontend/src/App.tsx</files>
  <action>
    - Create a robust `ErrorBoundary` class component in `frontend/src/components/ErrorBoundary.tsx`.
    - It should catch rendering errors, log them (optionally), and show a user-friendly fallback UI (e.g., "Something went wrong" with a Reload button).
    - Wrap the main application content in `frontend/src/App.tsx` with this `ErrorBoundary`.
  </action>
  <verify>
    - Add a temporary "Crash" button in a component to verify the `ErrorBoundary` catches the error and shows the fallback UI.
  </verify>
  <done>Global error handling is implemented at the root of the frontend application.</done>
</task>

<task type="auto">
  <name>Task 2: Enhanced Skeleton Loaders</name>
  <files>frontend/src/components/SkeletonTable.tsx, frontend/src/components/SkeletonChart.tsx</files>
  <action>
    - Enhance `frontend/src/components/SkeletonTable.tsx` using `motion` (framer-motion) to add pulsing or shimmering animations.
    - Create `frontend/src/components/SkeletonChart.tsx` to mimic the layout of the `TissueExpressionChart` (Recharts) during loading.
    - Use consistent Tailwind colors (e.g., `bg-gray-200` or custom variables) that will work with the upcoming dark mode.
  </action>
  <verify>
    - Inspect the components in the browser to ensure smooth `motion` animations are visible.
  </verify>
  <done>Animated skeleton loaders are available for tables and charts.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Loading States</name>
  <files>frontend/src/pages/ResultsPage.tsx, frontend/src/pages/GeneDetailPage.tsx, frontend/src/components/GeneTable.tsx, frontend/src/components/VariantTable.tsx</files>
  <action>
    - Update `ResultsPage.tsx` to display `SkeletonTable` while `useGeneSearch` or `useVariants` is loading.
    - Update `GeneDetailPage.tsx` to display `SkeletonChart` and metadata skeletons while data is fetching.
    - Ensure smooth transitions between skeleton and real data using `AnimatePresence` if appropriate.
    - Ensure the tables (`GeneTable`, `VariantTable`) handle the `isLoading` prop by rendering skeletons internally or having the parent handle it.
  </action>
  <verify>
    - Throttling the network in DevTools shows skeletons during the request and data after completion.
  </verify>
  <done>Loading states are visually represented across all major data-heavy views.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component → DOM | Error boundaries prevent single-component crashes from taking down the entire UI |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | D | Frontend | mitigate | Error boundary ensures availability of the rest of the application even if one module fails |
</threat_model>

<verification>
1. Manually trigger a render error and confirm the fallback UI appears.
2. Confirm skeletons are visible during network requests in Results and Detail pages.
</verification>

<success_criteria>
- Rendering errors are caught by the ErrorBoundary.
- Skeletons provide visual feedback during all data-fetching operations.
</success_criteria>

<output>
After completion, create `.planning/phases/05-production-deployment-polish/05-02-SUMMARY.md`
</output>
