---
phase: 02-variant-annotation-filtering
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [backend/api/variants.py, backend/services/gene_pipeline.py, frontend/src/components/VariantTable.tsx, frontend/src/hooks/useVariants.ts]
autonomous: false
requirements: [API-02, UI-02, UI-03, UI-04]
must_haves:
  truths:
    - "Filtering by CADD score updates the table without re-fetching all gene variants from external APIs"
    - "Impact levels (HIGH, MODERATE, LOW, MODIFIER) are visually distinct and color-coded"
    - "Virtualized table renders 1000+ variants smoothly"
  artifacts:
    - path: "backend/api/variants.py"
      provides: "DB-level filtered variant endpoint"
    - path: "frontend/src/components/VariantTable.tsx"
      provides: "High-density virtualized variant rendering"
  key_links:
    - from: "frontend/src/hooks/useVariants.ts"
      to: "backend/api/variants.py"
      via: "HTTP GET with filter params"
---

<objective>
Implement high-performance variant filtering at the database level and integrate it with the frontend UI. This plan ensures that the "zero-lag filtering" success criterion is met by leveraging PostgreSQL indexing and efficient TanStack Virtual rendering.

Key Improvements:
- Move filtering from Python to SQL for performance.
- Tighter integration between FilterPanel and VariantTable.
- Visual polish for Impact levels and CADD score bars.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-variant-annotation-filtering/02-CONTEXT.md
@.planning/phases/02-variant-annotation-filtering/02-RESEARCH.md
@.planning/phases/02-variant-annotation-filtering/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement DB-Level Filtering in Backend</name>
  <files>backend/services/gene_pipeline.py, backend/api/variants.py</files>
  <action>
    - Refactor `backend/services/gene_pipeline.py`: Update `get_variants_cached` to apply filters directly in the SQLAlchemy `select` statement using `VariantCache.cadd_score >= filters.get('cadd_min')`, etc.
    - Implement pagination (`.offset()`, `.limit()`) in the SQL query and return both the list of variants and the total count.
    - Update `backend/api/variants.py`: Replace the in-memory Python filtering with the results from the updated `get_variants_cached` method.
    - Pass all filter parameters (cadd, gerp, consequence, impact, regulome_max) to the pipeline.
  </action>
  <verify>
    <automated>pytest backend/tests/test_gene_pipeline.py backend/tests/test_genes_limit.py</automated>
  </verify>
  <done>Variant filtering is performed by PostgreSQL, not Python.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize Frontend Virtualization and Filtering</name>
  <files>frontend/src/hooks/useVariants.ts, frontend/src/components/VariantTable.tsx</files>
  <action>
    - Update `frontend/src/hooks/useVariants.ts`: Ensure `queryKey` includes all filter parameters so TanStack Query handles caching and re-fetching correctly.
    - Increase the default `limit` in the hook to support "high-density" display (e.g. 100 or 200 per page).
    - Update `frontend/src/components/VariantTable.tsx`: Refine the `TanStack Virtual` configuration. Ensure `estimateSize` is accurate (48px) and `overscan` is sufficient (20+).
    - Add a row-level highlighting for HIGH impact variants (e.g., light red background for the whole row).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Frontend renders large sets efficiently and highlights high-impact variants.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Functional Verification</name>
  <files>N/A</files>
  <action>Verify full variant filtering flow from UI to Database.</action>
  <what-built>Full variant filtering flow from UI to Database.</what-built>
  <how-to-verify>
    1. Search for a gene with many variants (e.g., "BRCA1").
    2. Switch to the Variation & Annotations tab.
    3. Use the Filter Panel to set CADD Score Min to 20.
    4. Observe that the table updates quickly and only shows rows with CADD >= 20.
    5. Verify that HIGH impact variants are visually highlighted.
    6. Scroll through the table to ensure zero lag in the virtualized rendering.
  </how-to-verify>
  <verify>Manual verification as described above.</verify>
  <resume-signal>approved</resume-signal>
  <done>Approved by user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted filter parameters |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | T | backend/api/variants.py | mitigate | Use Pydantic/FastAPI Query parameters to enforce numeric ranges and allowlisted strings |
| T-02-04 | D | backend/api/variants.py | mitigate | Limit the maximum `limit` parameter to 1000 to prevent database-side memory exhaustion |
</threat_model>

<verification>
Manual test with Chrome DevTools Network tab to confirm that filters are passed to the API and responses come back within <200ms.
</verification>

<success_criteria>
1. Filtering by CADD score updates the table in <500ms.
2. High-impact rows are visually distinct.
3. Virtual scrolling is smooth at 1000+ variants.
</success_criteria>

<output>
After completion, create `.planning/phases/02-variant-annotation-filtering/02-02-SUMMARY.md`
</output>
